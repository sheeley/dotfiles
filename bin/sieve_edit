#! /usr/bin/env bash
# Purpose: Edit email filtering rules for sieve mail filter
set -euo pipefail # x

# $EDITOR "$PRIVATE_TOOLS_DIR/bin/sieve.swift"
open "$PRIVATE_TOOLS_DIR/sieve-gen/Package.swift"
