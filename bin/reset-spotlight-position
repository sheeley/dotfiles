#! /usr/bin/env bash
set -euo pipefail

defaults delete com.apple.Spotlight lastWindowPosition
defaults delete com.apple.Spotlight lastVisibleScreenRect
defaults delete com.apple.Spotlight userHasMovedWindow
